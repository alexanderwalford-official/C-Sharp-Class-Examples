using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class itemdata : MonoBehaviour
{
    public string inspectiontext = "";
    public string itemtype = "inspect";

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
